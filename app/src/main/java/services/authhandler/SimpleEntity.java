package services.authhandler;

public class SimpleEntity {
    //insert fields wanted

    //Generate parameterized constructor with fields {this.thing = thing; on inside}

/*where want gson
    //be able get Content of request frmo json to readable using gson
    Gson gson = new Gson();
    SimpleEntity simpleEntity = gson.fromJson("jsonstringhere", SimpleEntity.class);
*/
}
